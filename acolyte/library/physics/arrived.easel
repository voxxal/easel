const CloseEnough=0.0001

pub await fn body.Arrived(prey, range=CloseEnough) {
	loop {
		await Tick

		let target = prey.Pos
		if !target { break }

		let bodyPos = body.Pos
		if !bodyPos { break }

		let distance = Length(target - bodyPos)
		if distance < range {
			break
		}
	}
}