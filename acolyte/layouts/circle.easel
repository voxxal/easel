pub fn layout.CircleLayout {
	SolidBackground(color=#2a1b2c)

	Spawn turf {
		Turf(color=#413243)
        StartingTurf(turf)
        FlashTurfOnVictory

		ShrinkOverTime scale {
			TurfShape = Circle(radius=32*scale)
		}
	}
}